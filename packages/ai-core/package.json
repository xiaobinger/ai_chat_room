{
  "name": "@tianma/ai-core",
  "private": true,
  "version": "0.0.0",
  "description": "调度与治理纯逻辑：状态机、预算、导演选人、管理员规则打分。无 IO，可完全单测。",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@tianma/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vitest": "^2.1.6"
  }
}
